<h1>メッセージ一覧</h1>

<ul>
  <% @messages.each do |message| %>
    <li><%= link_to message.id, message %> : <%= link_to message.content, message %></li>
  <% end %>
</ul>

<%= link_to '新規メッセージの投稿', new_message_path %>